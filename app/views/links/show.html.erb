<h2 class="page-header"><%= @link.title_or_url %></h1>

<%= link_to @link.url, @link.url %>

<div class="row mt-4">
  <div class="column md3 pl-0">
    <i class="fas fa-inbox mr-4 text-xl"></i>
    <%= @link.queue.name %>
  </div>
</div>
