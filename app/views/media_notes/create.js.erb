var template = '<%= escape_javascript(render 'media_items/note', note: @media_note) %>';
$find('#notes').insertAdjacentHTML('beforeend', template);

$find('#note-title').value = '';
$find('#notes form .ql-editor').innerText = '';
