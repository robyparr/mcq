<table>
  <thead>
    <tr>
      <th>Media</th>
      <th>Queue</th>
      <th>Created</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% media_items.each do |media_item| %>
      <tr>
        <td>
          <%= external_link_to media_item.title_or_url, media_item.url %>
          <div class="text-muted"><%= media_item.url %></div>
        </td>
        <td><%= media_item.queue.name %></td>
        <td><%= media_item.created_at %></td>
        <td class="actions">
          <%= link_to 'View', media_item_path(media_item) %>
          <%= link_to 'Edit', edit_media_item_path(media_item) %>
          <%= link_to 'Delete', media_item_path(media_item),
            method: :delete,
            data: { confirm: 'Are you sure?' } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
